export type TState = {
  name: string
}

const state: TState = {
  name: 'Skeleton'
}

const getters = {}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
